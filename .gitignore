*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# ── Downloaded at build time by app/download-models.sh ───────────────────────
# Large binary files auto-downloaded before compilation.
# Run: cd app && bash download-models.sh   (or just ./gradlew assembleDebug)
app/libs/
app/src/main/assets/piper-models/

# Signing keystore (never commit)
*.jks
*.keystore
