<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:orientation="vertical" android:background="@color/bg"
    android:padding="16dp">

    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="App Crashed"
        android:textColor="@color/status_error" android:textSize="20sp"
        android:fontFamily="monospace" android:layout_marginBottom="4dp"/>

    <TextView android:layout_width="match_parent" android:layout_height="wrap_content"
        android:text="The previous session ended unexpectedly.\nYou can report this crash to help fix it."
        android:textColor="@color/text_dim" android:textSize="13sp"
        android:fontFamily="monospace" android:layout_marginBottom="12dp"/>

    <ScrollView android:id="@+id/crash_scroll"
        android:layout_width="match_parent" android:layout_height="0dp"
        android:layout_weight="1" android:background="@color/surface"
        android:padding="12dp" android:layout_marginBottom="12dp">
        <TextView android:id="@+id/crash_log_text"
            android:layout_width="match_parent" android:layout_height="wrap_content"
            android:textColor="@color/text_primary" android:textSize="11sp"
            android:fontFamily="monospace" android:textIsSelectable="true"/>
    </ScrollView>

    <LinearLayout android:layout_width="match_parent" android:layout_height="wrap_content"
        android:orientation="horizontal" android:gravity="end">

        <Button android:id="@+id/btn_dismiss"
            android:layout_width="wrap_content" android:layout_height="40dp"
            android:text="Dismiss" android:textSize="13sp"
            android:backgroundTint="@color/surface" android:textColor="@color/text_dim"
            android:layout_marginEnd="8dp"/>

        <Button android:id="@+id/btn_copy_log"
            android:layout_width="wrap_content" android:layout_height="40dp"
            android:text="Copy Log" android:textSize="13sp"
            android:backgroundTint="@color/surface" android:textColor="@color/text_primary"
            android:layout_marginEnd="8dp"/>

        <Button android:id="@+id/btn_report_github"
            android:layout_width="wrap_content" android:layout_height="40dp"
            android:text="Report to GitHub" android:textSize="13sp"
            android:backgroundTint="#1a3a1a" android:textColor="@color/green"/>
    </LinearLayout>
</LinearLayout>
